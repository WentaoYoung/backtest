# Optional: pull any legacy Git LFS blobs if .git is present in the build image.
[phases.setup]
nixPkgs = ["git", "git-lfs"]

[phases.build]
cmds = [
  "git lfs install 2>/dev/null || true",
  "git lfs pull 2>/dev/null || true",
]
