data/market_value.csv filter=lfs diff=lfs merge=lfs -text
data/*.csv filter=lfs diff=lfs merge=lfs -text
# Parquet shards are each <100MB — store as normal git blobs (Railway does not pull LFS by default).
